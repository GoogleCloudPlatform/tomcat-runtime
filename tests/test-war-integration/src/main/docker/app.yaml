runtime: custom
env: flex
service: tomcat-runtime-integration

health_check:
  enable_health_check: False

manual_scaling:
  instances: 1

env_variables:
  TOMCAT_MODULES_ENABLE: trace-requests

handlers:
- url: /.*
  script: ignored
  secure: optional