steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/${docker.image.name}:${docker.tag.long}', 'tomcat/target/docker-src']

images:
- 'gcr.io/$PROJECT_ID/${docker.image.name}:${docker.tag.long}'
